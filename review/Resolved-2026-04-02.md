# Resolved Items - 2026-04-02

## P0/P1 items completed

1. Core layer dependency violation fixed in permission handling.
- `core.permission.PermissionHandler` no longer imports presentation ViewModel.
- Introduced `PermissionStateSink` boundary and adapter wiring in launcher runtime.

2. Search suggestion resolver duplication removed.
- Shared logic extracted into `SuggestionPatternMatcher`.
- Both query and clipboard resolvers now reuse common URL/phone/map parsing.

3. App shortcut launch TODO addressed.
- Implemented `LauncherApps.startShortcut(...)` path with fallback.

4. Dead empty utility removed.
- Deleted `SearchProviderUtils` placeholder object.

5. Duplicate launch intent builder removed.
- Added shared launcher intent helper and replaced duplicated methods in app repositories.

6. Unused URL item callback removed.
- Simplified `UrlSearchResultItem` API and updated callsites.

7. Placeholder unit test removed.
- Deleted `ExampleUnitTest` template test.

8. Release hardening status verified.
- Release minification and resource shrinking are enabled in `app/build.gradle.kts`.

9. New coverage added for shared matcher.
- Added `SuggestionPatternMatcherTest` for phone normalization and map detection heuristics.

10. Exception-handling hardening started in top execution hotspot.
- Refactored `ActionExecutor` to replace broad exception catches with specific exceptions and added warning logs.
- Reduced broad catch count in main source from 32 to 24.

11. Home mutation performance instrumentation added.
- Added slow-mutation timing logs in `HomeMutationCoordinator` to identify real bottlenecks before queue redesign.

12. Serialized mutation queue bottleneck addressed.
- Replaced mutex-gated mutation execution with a single-consumer channel queue.
- Added keyed coalescing so rapid repeated move/update requests keep only the latest queued mutation per item/widget.
- Added targeted unit coverage in `HomeMutationCoordinatorTest` for coalescing behavior.

package com.milki.launcher.domain.model

/**
 * Backward-compatible alias retained for existing call sites.
 *
 * Source and provider prefix mutations now share the same result contract via
 * [PrefixMutationResult].
 */
typealias SourcePrefixMutationResult = PrefixMutationResult

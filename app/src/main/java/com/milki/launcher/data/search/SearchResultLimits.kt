package com.milki.launcher.data.search

/**
 * Maximum number of search results any provider returns per query.
 *
 * Used across the search providers (contacts, files) so every result surface
 * stays consistent with one shared limit.
 */
const val MAX_SEARCH_RESULTS = 10